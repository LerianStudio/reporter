services:
  manager:
    container_name: plugin-template-engine-manager
    restart: always
    build:
      context: ../../
      dockerfile: ./components/manager/Dockerfile
    env_file:
      - .env
    ports:
      - ${SERVER_PORT}:${SERVER_PORT}
    volumes:
      - .:/usr/src/app
    networks:
      - plugin-template-engine-infra-network
      - plugin-template-engine-manager-network

networks:
  plugin-template-engine-infra-network:
    name: plugin-template-engine-infra-network
    driver: bridge
    external: true
  plugin-template-engine-manager-network:
    name: plugin-template-engine-manager-network
    driver: bridge