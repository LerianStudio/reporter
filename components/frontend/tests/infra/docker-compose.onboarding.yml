name: reporter-test

services:
  midaz-onboarding-test:
    container_name: midaz-onboarding-test
    image: lerianstudio/midaz-onboarding:latest
    env_file:
      - .env.onboarding
    ports:
      - 3000:3000
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '--quiet',
          '--tries=1',
          '--spider',
          'http://localhost:3000/health'
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - reporter-test-network

networks:
  reporter-test-network:
    name: reporter-test-network
    driver: bridge
    external: true
